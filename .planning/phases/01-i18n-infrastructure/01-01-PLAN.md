---
phase: 01-i18n-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - src/app.html
  - src/hooks.server.ts
  - src/routes/+layout.svelte
  - src/lib/components/layout/AppSidebar.svelte
  - src/lib/components/layout/LanguageSwitcher.svelte
  - src/lib/components/ui/sonner/index.ts
  - src/lib/components/ui/sonner/sonner.svelte
  - messages/en.json
  - messages/de.json
  - project.inlang/settings.json
  - .gitignore
  - package.json
autonomous: true
requirements:
  - I18N-01
  - I18N-03
  - I18N-07
  - I18N-08
  - I18N-09

must_haves:
  truths:
    - "A LanguageSwitcher dropdown is visible in the sidebar showing 'English' and 'Deutsch'"
    - "Clicking a language triggers a page reload and the UI switches locale"
    - "The HTML lang attribute matches the active locale (e.g., lang='de')"
    - "Selecting German persists across browser close/reopen (cookie)"
    - "On first visit with no cookie, browser Accept-Language determines the starting locale"
    - "A toast notification can be triggered from any component and renders in the root layout"
  artifacts:
    - path: "vite.config.ts"
      provides: "Paraglide Vite plugin with cookie+preferredLanguage+baseLocale strategy"
      contains: "paraglideVitePlugin"
    - path: "project.inlang/settings.json"
      provides: "Inlang project config: baseLocale en, locales [en, de]"
      contains: "baseLocale"
    - path: "src/app.html"
      provides: "HTML lang placeholder for server-side locale injection"
      contains: "%lang%"
    - path: "src/hooks.server.ts"
      provides: "Paraglide middleware wired via sequence()"
      contains: "paraglideMiddleware"
    - path: "src/lib/components/layout/LanguageSwitcher.svelte"
      provides: "Locale selector using getLocale/setLocale/locales from paraglide runtime"
      contains: "setLocale"
    - path: "src/lib/components/layout/AppSidebar.svelte"
      provides: "Sidebar containing LanguageSwitcher"
      contains: "LanguageSwitcher"
    - path: "src/routes/+layout.svelte"
      provides: "Root layout with Toaster mounted"
      contains: "Toaster"
    - path: "messages/en.json"
      provides: "English message keys"
    - path: "messages/de.json"
      provides: "German message keys"
  key_links:
    - from: "src/hooks.server.ts"
      to: "src/app.html"
      via: "transformPageChunk replacing %lang% with detected locale"
      pattern: "html\\.replace\\('%lang%'"
    - from: "src/lib/components/layout/AppSidebar.svelte"
      to: "src/lib/components/layout/LanguageSwitcher.svelte"
      via: "import and render"
      pattern: "LanguageSwitcher"
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/ui/sonner"
      via: "Toaster import and render"
      pattern: "Toaster"
---

<objective>
Bring the Paraglide i18n infrastructure from the `feature/paraglide-i18n` worktree branch into the main `features` branch, then add the missing Sonner toast component.

Purpose: The worktree branch completed all Paraglide wiring (vite plugin, inlang config, hooks middleware, app.html placeholder, message files, LanguageSwitcher component). Sonner was not installed. This plan merges that work and completes the infrastructure so every Phase 1 requirement is satisfied before verification.

Output:
- All Paraglide files merged into `features`
- Sonner installed and Toaster mounted in root layout
- `bun dev` starts without errors, Paraglide compiles successfully
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i18n-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge worktree branch and fix sequence order</name>
  <files>
    vite.config.ts
    src/app.html
    src/hooks.server.ts
    src/lib/components/layout/AppSidebar.svelte
    src/lib/components/layout/LanguageSwitcher.svelte
    src/routes/+layout.svelte
    messages/en.json
    messages/de.json
    project.inlang/settings.json
    .gitignore
    package.json
    bun.lock
  </files>
  <action>
    The `feature/paraglide-i18n` worktree branch (at `.worktrees/paraglide-i18n`) contains completed Paraglide infrastructure. Merge it into the current `features` branch:

    ```bash
    git merge feature/paraglide-i18n --no-ff -m "feat(i18n): merge paraglide infrastructure from worktree"
    ```

    If merge conflicts occur, resolve them by keeping the worktree branch changes for any paraglide-related files and keeping the `features` branch changes for unrelated files.

    After merging, fix the `sequence()` order in `src/hooks.server.ts`. The worktree has `sequence(paraglideHandle, appHandle)` but per the research, `appHandle` (which sets `event.locals.user`) MUST run first so that session data is available to any downstream code. Change the last line to:

    ```typescript
    export const handle = sequence(appHandle, paraglideHandle);
    ```

    Also verify `src/routes/+layout.svelte` in the merged state. The worktree's layout already imports `* as m from '$lib/paraglide/messages'` for SEO title/description. This is correct — leave it. The layout does NOT yet have Toaster — Task 2 adds it.

    Run `bun install` to install the newly added `@inlang/paraglide-js` dependency from the merged `package.json`.
  </action>
  <verify>
    - `git log --oneline -5` shows the merge commit
    - `cat src/hooks.server.ts | grep "sequence"` shows `sequence(appHandle, paraglideHandle)` (appHandle first)
    - `cat src/app.html | grep "lang="` shows `lang="%lang%"`
    - `cat vite.config.ts | grep "paraglideVitePlugin"` shows the plugin present
    - `cat src/lib/components/layout/LanguageSwitcher.svelte | grep "setLocale"` shows the component
    - `ls messages/` shows `en.json` and `de.json`
    - `bun install` exits 0
  </verify>
  <done>
    The `features` branch contains all Paraglide infrastructure files from the worktree branch. `sequence` order is `(appHandle, paraglideHandle)`. `bun install` succeeds with `@inlang/paraglide-js` present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Sonner and mount Toaster in root layout</name>
  <files>
    src/lib/components/ui/sonner/index.ts
    src/lib/components/ui/sonner/sonner.svelte
    src/routes/+layout.svelte
    package.json
    bun.lock
  </files>
  <action>
    Install Sonner via the shadcn-svelte CLI (this installs `svelte-sonner` as a runtime dependency and drops the Toaster component into `$lib/components/ui/sonner/`):

    ```bash
    bunx shadcn-svelte@latest add sonner
    ```

    Accept all prompts (press Enter / y for any confirmation).

    After installation, open `src/routes/+layout.svelte` and add the Toaster:

    1. Add import at top of `<script>`:
       ```typescript
       import { Toaster } from '$lib/components/ui/sonner/index.js';
       ```

    2. Add `<Toaster />` AFTER the closing `</Tooltip.Provider>` tag (not inside it). The Toaster must be at the root level, always mounted, so its portal and z-index work correctly. Do NOT place it inside any conditional block.

    The final structure of the template section should look like:
    ```svelte
    <Tooltip.Provider>
      <div class="h-screen bg-slate-100 flex flex-col overflow-hidden" style="height: 100dvh;">
        {@render children()}
      </div>
      <AppSidebar />
    </Tooltip.Provider>

    <Toaster />
    ```

    Do not change any other part of `+layout.svelte`.
  </action>
  <verify>
    - `ls src/lib/components/ui/sonner/` shows `index.ts` (or `index.svelte`) and a Toaster component file
    - `grep "svelte-sonner" package.json` shows the dependency present
    - `grep "Toaster" src/routes/+layout.svelte` shows the import and usage
    - `grep "Toaster" src/routes/+layout.svelte | wc -l` returns at least 2 (import + render)
  </verify>
  <done>
    `svelte-sonner` is installed. `src/lib/components/ui/sonner/` contains the shadcn-wrapped Toaster component. `src/routes/+layout.svelte` imports and renders `<Toaster />` outside the `Tooltip.Provider` wrapper.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify dev server starts and Paraglide compiles</name>
  <files></files>
  <action>
    Run a build to confirm everything compiles without errors:

    ```bash
    bun build 2>&1
    ```

    Watch for:
    - `✔ Compilation complete` from the Paraglide compiler (or equivalent success message)
    - No TypeScript errors from paraglide imports (`$lib/paraglide/runtime`, `$lib/paraglide/messages`)
    - No Svelte component errors in `LanguageSwitcher.svelte` or `+layout.svelte`

    If `bun build` fails with "Cannot find module '$lib/paraglide/runtime'" errors, it means the Paraglide compiler hasn't run yet. In that case run:

    ```bash
    bunx @inlang/paraglide-js compile --project ./project.inlang --outdir ./src/lib/paraglide
    ```

    Then re-run `bun build`.

    If `bun check` reports errors about `src/lib/paraglide/_index.js` (known issue in the in-progress doc), check if the error is in the generated file itself or in a component importing from it. If it's in the generated file — ignore it (do not edit generated files). If it's in a component, fix the import path to use the `.js` extension: `from '$lib/paraglide/messages.js'` not `from '$lib/paraglide/messages'`.

    Commit all changes:
    ```bash
    git add -A
    git commit -m "feat(i18n): add sonner toast infrastructure to layout"
    ```
  </action>
  <verify>
    - `bun build` exits with code 0 (or the only errors are pre-existing, unrelated ones)
    - `git log --oneline -3` shows the commit for sonner addition
    - `ls src/lib/paraglide/` shows generated files: `messages.js`, `runtime.js`, `server.js` (names may vary)
  </verify>
  <done>
    The project builds successfully. Generated paraglide files exist in `src/lib/paraglide/`. All Phase 1 infrastructure is committed to the `features` branch.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `cat src/app.html` — contains `lang="%lang%"` (not `lang="en"`)
2. `cat src/hooks.server.ts | grep sequence` — shows `sequence(appHandle, paraglideHandle)`
3. `cat vite.config.ts | grep strategy` — shows `['cookie', 'preferredLanguage', 'baseLocale']`
4. `cat src/lib/components/layout/LanguageSwitcher.svelte` — uses `getLocale`, `setLocale`, `locales` from `$lib/paraglide/runtime`
5. `grep Toaster src/routes/+layout.svelte` — present
6. `ls src/lib/components/ui/sonner/` — Toaster component files exist
7. `bun build` — exits 0
</verification>

<success_criteria>
- All Paraglide infrastructure files are on the `features` branch (merged from worktree)
- `sequence(appHandle, paraglideHandle)` order in hooks.server.ts
- Sonner installed and Toaster in root layout
- Project builds without errors
- `src/lib/paraglide/` is in `.gitignore` and not tracked by git
</success_criteria>

<output>
After completion, create `.planning/phases/01-i18n-infrastructure/01-01-SUMMARY.md` following the summary template.
</output>
