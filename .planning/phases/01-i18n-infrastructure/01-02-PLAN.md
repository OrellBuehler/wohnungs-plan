---
phase: 01-i18n-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: false
requirements:
  - I18N-01
  - I18N-03
  - I18N-07
  - I18N-08
  - I18N-09

must_haves:
  truths:
    - "Language switcher is visible in the sidebar and shows 'English' / 'Deutsch' as options"
    - "Selecting a different language reloads the page and the UI reflects the new locale"
    - "After switching to German, closing and reopening the browser still shows German"
    - "The HTML lang attribute in DevTools matches the active locale"
    - "First visit with no cookie auto-detects browser language"
    - "A toast notification renders correctly when triggered"
  artifacts:
    - path: "src/lib/components/layout/LanguageSwitcher.svelte"
      provides: "Rendered dropdown in sidebar"
    - path: "src/routes/+layout.svelte"
      provides: "Toaster mounted and visible"
  key_links:
    - from: "PARAGLIDE_LOCALE cookie"
      to: "locale detection on reload"
      via: "cookie strategy in vite.config.ts"
      pattern: "cookie"
    - from: "Accept-Language header"
      to: "first-visit locale"
      via: "preferredLanguage strategy"
      pattern: "preferredLanguage"
---

<objective>
Human verification that all Phase 1 infrastructure requirements are met end-to-end: language switching, locale persistence, HTML lang attribute, browser language auto-detection, and toast notifications.

Purpose: Infrastructure can compile but fail silently at runtime. This checkpoint confirms the full user-facing flow before Phase 2 string extraction begins on top of it.

Output: Confirmed or documented issues for gap closure.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-i18n-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 1 i18n infrastructure end-to-end</name>
  <action>
    This is a human verification checkpoint. Run `bun dev` and open the app at http://localhost:5173. Verify each requirement:

    **What was built (from Plan 01):**
    - Paraglide v2 wired via Vite plugin (cookie + preferredLanguage + baseLocale strategy)
    - HTML lang placeholder in app.html replaced server-side per locale
    - Language switcher dropdown in the sidebar (English / Deutsch)
    - PARAGLIDE_LOCALE cookie for session persistence
    - Sonner Toaster mounted in root layout

    **I18N-01 + I18N-09: Language switcher shows native names**
    1. Open the sidebar (hamburger menu or sidebar icon)
    2. Confirm a language selector shows "English" and "Deutsch" as options
    3. The current locale should be pre-selected

    **I18N-01: Switching language changes the UI**
    4. Select "Deutsch" from the switcher
    5. The page should reload automatically
    6. Confirm the sidebar and any translated strings now show German text

    **I18N-07: HTML lang attribute updates**
    7. After switching to German, open DevTools (F12) → Elements tab
    8. Find the `<html>` tag at the top of the DOM
    9. Confirm it shows `lang="de"` (not `lang="en"` or `lang="%lang%"`)

    **I18N-03: Locale persists across browser close**
    10. With German selected, close the browser tab entirely
    11. Reopen http://localhost:5173 in a fresh tab
    12. Confirm the UI still shows German
    13. In DevTools → Application → Cookies, find `PARAGLIDE_LOCALE` cookie set to `de`

    **I18N-08: Browser auto-detection**
    14. In DevTools → Application → Cookies, delete the `PARAGLIDE_LOCALE` cookie
    15. Hard-reload (Ctrl+Shift+R / Cmd+Shift+R)
    16. Confirm the locale matches your browser's Accept-Language setting

    **Sonner toast smoke test**
    17. Confirm `src/lib/components/ui/sonner/` exists with component files
    18. Optionally: temporarily add `import { toast } from 'svelte-sonner'; toast.success('Test')` to any component to confirm toast renders in the corner of the screen

    Type "approved" if all checks pass, or describe any failures.
  </action>
  <verify>
    Human confirms via "approved" response.
  </verify>
  <done>
    All five Phase 1 ROADMAP success criteria are observable in the running app:
    1. Language switcher visible in sidebar with native names ("English", "Deutsch")
    2. Switching locale reloads page and UI changes
    3. HTML lang attribute in DevTools matches active locale
    4. PARAGLIDE_LOCALE cookie persists across browser close
    5. Toaster component installed and mounted in root layout
  </done>
</task>

</tasks>

<verification>
All Phase 1 success criteria verified by human:
1. Language switcher visible in sidebar with "English" and "Deutsch"
2. Switching locale reloads page and UI changes
3. `lang` attribute in HTML matches active locale
4. PARAGLIDE_LOCALE cookie persists across browser close
5. Browser language auto-detection works on cookie-less first visit
6. Toaster component is installed and mounted
</verification>

<success_criteria>
Human confirms all five Phase 1 success criteria from ROADMAP.md are observable:
1. Language switcher visible in sidebar with native names
2. Locale persists after browser close (cookie)
3. HTML lang attribute reflects active locale
4. First visit auto-detects browser language
5. Toast infrastructure in place (Toaster mounted in root layout)
</success_criteria>

<output>
After completion, create `.planning/phases/01-i18n-infrastructure/01-02-SUMMARY.md` following the summary template.
</output>
