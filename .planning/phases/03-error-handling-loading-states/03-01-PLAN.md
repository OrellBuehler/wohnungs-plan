---
phase: 03-error-handling-loading-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/de.json
  - src/lib/stores/project.svelte.ts
  - src/lib/stores/collaboration.svelte.ts
  - src/lib/stores/comments.svelte.ts
  - src/lib/stores/auth.svelte.ts
  - src/routes/+page.svelte
autonomous: true
requirements:
  - ERRH-01
  - ERRH-02
  - ERRH-03
  - ERRH-06

must_haves:
  truths:
    - "Network errors during save/load surface as visible toast messages"
    - "Fire-and-forget API calls in project store show toast on failure"
    - "Project load failure shows error toast"
    - "Logged-out user visiting a project URL sees a login message"
  artifacts:
    - path: "messages/en.json"
      provides: "Error message keys in English"
      contains: "error_network"
    - path: "messages/de.json"
      provides: "Error message keys in German"
      contains: "error_network"
    - path: "src/lib/stores/project.svelte.ts"
      provides: "Toast error calls on all API failures"
      contains: "toast.error"
  key_links:
    - from: "src/lib/stores/project.svelte.ts"
      to: "svelte-sonner"
      via: "toast.error() in catch blocks"
      pattern: "toast\\.error"
    - from: "src/routes/+page.svelte"
      to: "auth redirect"
      via: "reason query param display"
      pattern: "auth.*required|reason"
---

<objective>
Wire toast error notifications into all silent failure paths across stores, add i18n error message keys, and add auth redirect messaging.

Purpose: Eliminate silent failures — every API error becomes visible to the user as a toast notification. Logged-out users see a clear message instead of a blank redirect.

Output: All store catch blocks and fire-and-forget calls surface errors via toast. Auth redirect shows reason message on home page.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-handling-loading-states/03-RESEARCH.md

@messages/en.json
@messages/de.json
@src/lib/stores/project.svelte.ts
@src/lib/stores/auth.svelte.ts
@src/lib/stores/collaboration.svelte.ts
@src/lib/stores/comments.svelte.ts
@src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error i18n keys and wire toast.error() into all store failure paths</name>
  <files>
    messages/en.json
    messages/de.json
    src/lib/stores/project.svelte.ts
    src/lib/stores/collaboration.svelte.ts
    src/lib/stores/comments.svelte.ts
  </files>
  <action>
Add error message keys to both en.json and de.json for every error surface identified in the research audit. Use the existing flat snake_case naming convention with `error_` prefix. Keys needed (at minimum):
- `error_network` / "Network error — please try again" / "Netzwerkfehler — bitte erneut versuchen"
- `error_load_projects` / "Could not load projects" / "Projekte konnten nicht geladen werden"
- `error_load_project` / "Could not load project" / "Projekt konnte nicht geladen werden"
- `error_save_project` / "Could not save project" / "Projekt konnte nicht gespeichert werden"
- `error_create_project` / "Could not create project" / "Projekt konnte nicht erstellt werden"
- `error_delete_project` / "Could not delete project" / "Projekt konnte nicht geloescht werden"
- `error_update_item` / "Could not update item" / "Element konnte nicht aktualisiert werden"
- `error_delete_item` / "Could not delete item" / "Element konnte nicht geloescht werden"
- `error_add_item` / "Could not add item" / "Element konnte nicht hinzugefuegt werden"
- `error_duplicate_item` / "Could not duplicate item" / "Element konnte nicht dupliziert werden"
- `error_upload_floorplan` / "Could not upload floorplan" / "Grundriss konnte nicht hochgeladen werden"
- `error_upload_image` / "Could not upload image" / "Bild konnte nicht hochgeladen werden"
- `error_delete_image` / "Could not delete image" / "Bild konnte nicht geloescht werden"
- `error_sync` / "Could not sync changes" / "Aenderungen konnten nicht synchronisiert werden"
- `error_branch_create` / "Could not create branch" / "Branch konnte nicht erstellt werden"
- `error_branch_rename` / "Could not rename branch" / "Branch konnte nicht umbenannt werden"
- `error_branch_delete` / "Could not delete branch" / "Branch konnte nicht geloescht werden"
- `error_update_scale` / "Could not update scale" / "Massstab konnte nicht aktualisiert werden"
- `error_clear_floorplan` / "Could not clear floorplan" / "Grundriss konnte nicht entfernt werden"
- `error_form_submission` / "Please fix the errors above" / "Bitte beheben Sie die oben angezeigten Fehler"

Use proper German characters (oe->ö, ue->ü, ae->ä) in the actual de.json values.

Then wire `toast.error()` into `src/lib/stores/project.svelte.ts`:
- Import `toast` from `svelte-sonner` and `* as m` from `$lib/paraglide/messages`
- IMPORTANT per research Pitfall 2: Since store files may run during SSR, guard toast calls with `if (typeof window !== 'undefined')` OR only call toast from the component layer. The recommended approach from research is: make store functions throw/return error indicators, and call toast from the component. However, the simpler approach for fire-and-forget `void authFetch(...)` calls is to add `.catch()` handlers that include the toast call, since these only run client-side (they're called from event handlers in components). Use this approach:
  - For every `void authFetch(...)` pattern (9 identified in research), chain a `.catch(() => { toast.error(m.error_xxx()) })`
  - For every `catch` block that only has `console.error` (9 identified in research), add `toast.error(m.error_xxx())` after the console.error (keep console.error for debugging)

Do the same for `collaboration.svelte.ts` and `comments.svelte.ts` where they have `console.error`-only catch blocks.
  </action>
  <verify>
Run `bun check` to verify no type errors. Grep for `toast.error` in project.svelte.ts to confirm at least 15 occurrences. Grep for `error_` in en.json to confirm at least 15 error keys.
  </verify>
  <done>Every catch block and fire-and-forget API call in stores surfaces a toast.error() with a translated message. No silent failures remain in project.svelte.ts, collaboration.svelte.ts, or comments.svelte.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth redirect reason message and form error display pattern</name>
  <files>
    src/lib/stores/auth.svelte.ts
    src/routes/+page.svelte
    messages/en.json
    messages/de.json
  </files>
  <action>
**Auth redirect (ERRH-06):**
1. In `src/lib/stores/auth.svelte.ts`, find `handleUnauthorized()` which redirects to `/api/auth/login`. Update it to include a `reason=auth_required` query parameter: redirect to `/api/auth/login?reason=auth_required`. Keep the existing behavior otherwise.

2. In `src/routes/+page.svelte` (the home/login page), check for `?reason=auth_required` query param on mount. If present, show a toast: `toast.info(m.auth_login_required())`. Add i18n keys:
  - `auth_login_required` / "Please log in to continue" / "Bitte melden Sie sich an, um fortzufahren"

3. Clear the query param after showing the toast (use `replaceState` from `$app/navigation` to remove the param without navigation).

**Form inline errors (ERRH-02):**
The app uses SvelteKit form actions and client-side validation. Add the `error_form_submission` key (already in Task 1). For inline form errors, the existing pattern of reactive error state in Svelte 5 (`$state` for field errors) is already in place for validation in ItemForm and similar components. Verify that form submission errors already display inline — if they do, this requirement is met by existing code. If any form lacks inline error display, add a reactive `$state` error variable and display it next to the field with red text (`text-destructive text-sm`).

**Project load error (ERRH-03):**
Check if the home page already has an error state with retry. Per research, it does. Check if project detail page `/projects/[id]/+page.svelte` has error handling when `loadProjectById` fails. If it just redirects to `/` silently, add a toast.error before the redirect: `toast.error(m.error_load_project())`.
  </action>
  <verify>
Run `bun check`. Visit the home page with `?reason=auth_required` and verify a toast appears (manual check during phase verification). Grep for `auth_login_required` in en.json.
  </verify>
  <done>Logged-out users redirected to login see a "Please log in" message. Form submission errors display inline. Project load failures surface a toast before redirecting.</done>
</task>

</tasks>

<verification>
1. `bun check` passes with no type errors
2. `bun build` succeeds
3. Grep confirms toast.error calls exist in project.svelte.ts (15+), collaboration.svelte.ts, comments.svelte.ts
4. Grep confirms error_ keys in en.json and de.json (15+)
5. Auth redirect includes reason param
</verification>

<success_criteria>
- Zero silent API failures in stores — every catch block and fire-and-forget call has a toast.error
- Error messages are translated in both EN and DE
- Auth redirect shows a meaningful login message
- Form errors display inline
- All type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-handling-loading-states/03-01-SUMMARY.md`
</output>
