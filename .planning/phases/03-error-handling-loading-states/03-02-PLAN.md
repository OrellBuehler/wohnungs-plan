---
phase: 03-error-handling-loading-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/projects/[id]/+page.svelte
  - src/routes/projects/[id]/history/+page.svelte
  - src/lib/components/shared/OfflineBadge.svelte
  - src/lib/components/Header.svelte
  - messages/en.json
  - messages/de.json
autonomous: true
requirements:
  - VISD-03
  - ERRH-07

must_haves:
  truths:
    - "Project detail page shows skeleton loading cards while data is fetching"
    - "Item list shows skeleton placeholders while loading"
    - "Offline sync queue badge shows pending change count when offline"
  artifacts:
    - path: "src/routes/projects/[id]/+page.svelte"
      provides: "Skeleton loading state for project detail"
      contains: "animate-pulse"
    - path: "src/lib/components/shared/OfflineBadge.svelte"
      provides: "Offline pending changes badge"
      contains: "pendingChanges"
  key_links:
    - from: "src/lib/components/shared/OfflineBadge.svelte"
      to: "src/lib/stores/sync.svelte.ts"
      via: "getPendingChanges and isOnline imports"
      pattern: "getPendingChanges|isOnline"
    - from: "src/lib/components/Header.svelte"
      to: "src/lib/components/shared/OfflineBadge.svelte"
      via: "component import and render"
      pattern: "OfflineBadge"
---

<objective>
Add skeleton loading states to all data-fetching views and an offline sync queue badge to the header.

Purpose: Users see immediate visual feedback while data loads (no blank flashes) and can see how many changes are queued when offline.

Output: Skeleton placeholders on project detail and item list views. Offline badge in header showing pending change count.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-handling-loading-states/03-RESEARCH.md

@src/routes/+page.svelte
@src/routes/projects/[id]/+page.svelte
@src/routes/projects/[id]/history/+page.svelte
@src/lib/stores/sync.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading states to project detail and item list views</name>
  <files>
    src/routes/projects/[id]/+page.svelte
    src/routes/projects/[id]/history/+page.svelte
  </files>
  <action>
**Project detail page (`src/routes/projects/[id]/+page.svelte`):**
The research notes this page already has "Header + canvas skeleton — already implemented". Verify the existing skeleton covers the loading state properly. If the skeleton only covers the header and canvas but not the sidebar item list, add skeleton placeholders for the sidebar:
- When loading, show 3-4 skeleton item cards in the sidebar area using `animate-pulse` with `bg-muted rounded-md` (same pattern as home page)
- Simple card-shaped rectangles as per user decision — no internal layout detail
- Use shimmer/pulse animation (Tailwind `animate-pulse`)

Check the existing loading state variable (likely a boolean like `loading` or `isLoading`). Use the same variable to gate the skeleton display.

**History page (`src/routes/projects/[id]/history/+page.svelte`):**
Check if this page has any loading state. If it loads data and shows a blank while loading, add a skeleton table:
- Show 5-6 skeleton rows with `animate-pulse` rectangles matching the table column layout
- Use `bg-muted rounded` for each cell placeholder

**Home page (`src/routes/+page.svelte`):**
Per research, the home page already has 6 skeleton cards. Verify they use `animate-pulse`. If so, this is already complete for VISD-03.

All skeleton styles should use the existing project convention: `bg-muted` background with `animate-pulse` and `rounded-md` shape.
  </action>
  <verify>
Run `bun check`. Grep for `animate-pulse` in project page and history page to confirm skeleton elements exist.
  </verify>
  <done>All data-fetching views (home page, project detail, item list, history) show skeleton loading placeholders while data loads. No blank flashes.</done>
</task>

<task type="auto">
  <name>Task 2: Add offline sync queue badge to header</name>
  <files>
    src/lib/components/shared/OfflineBadge.svelte
    src/lib/components/Header.svelte
    messages/en.json
    messages/de.json
  </files>
  <action>
**Create `src/lib/components/shared/OfflineBadge.svelte`:**
- Import `getPendingChanges`, `isOnline` from `$lib/stores/sync.svelte`
- Import `Badge` from `$lib/components/ui/badge`
- Import `* as m` from `$lib/paraglide/messages`
- Use `$derived` to reactively get `pending = getPendingChanges()` and `online = isOnline()`
- Show the badge only when `!online && pending > 0`
- Display the pending count inside a Badge with `variant="secondary"` or `variant="outline"`
- Add a tooltip or title attribute: `m.offline_pending_changes({ count: pending })`
- Add i18n keys:
  - `offline_pending_changes` / "{count} pending" / "{count} ausstehend"
  - `offline_status` / "Offline" / "Offline"

**Wire into header:**
- Find the Header component (check `src/lib/components/Header.svelte` or equivalent — could be in `+layout.svelte` or a shared component). Look for existing header elements.
- Import and render `<OfflineBadge />` in the header, positioned near the right side alongside existing header controls.
- Also add the badge to the mobile bottom navigation/tabs if it exists, so mobile users see it too.

The badge should be subtle but visible — use a small Badge with the count number. When online, the badge disappears entirely.
  </action>
  <verify>
Run `bun check`. Verify OfflineBadge.svelte exists and imports sync store functions. Verify Header component imports OfflineBadge.
  </verify>
  <done>When offline with pending changes, a badge appears in the header showing the count. When online or with no pending changes, the badge is hidden.</done>
</task>

</tasks>

<verification>
1. `bun check` passes
2. `bun build` succeeds
3. Project detail page has skeleton loading state with `animate-pulse`
4. OfflineBadge.svelte exists and uses sync store
5. Header renders OfflineBadge component
</verification>

<success_criteria>
- Project detail, item list, and history pages show skeleton loading states
- Offline badge appears in header when offline with pending changes
- Badge disappears when online
- All type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-handling-loading-states/03-02-SUMMARY.md`
</output>
