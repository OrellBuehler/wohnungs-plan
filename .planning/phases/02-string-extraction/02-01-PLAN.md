---
phase: 02-string-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/format.ts
  - src/lib/utils/currency.ts
  - messages/en.json
  - messages/de.json
autonomous: true
requirements: [I18N-04, I18N-05, I18N-10]

must_haves:
  truths:
    - "formatDecimal() returns locale-correct decimal separators (comma for DE, dot for EN)"
    - "formatDimension() returns locale-formatted dimension string with × separator and cm unit"
    - "formatRelativeTime() returns translated relative time using paraglide message keys"
    - "formatPrice() uses Intl.NumberFormat with getLocale() instead of .toFixed(2)"
    - "All message keys needed by Plans 02 and 03 exist in both en.json and de.json"
  artifacts:
    - path: "src/lib/utils/format.ts"
      provides: "formatDecimal, formatDimension, formatRelativeTime utilities"
      contains: "Intl.NumberFormat"
    - path: "src/lib/utils/currency.ts"
      provides: "Locale-aware formatPrice"
      contains: "Intl.NumberFormat"
    - path: "messages/en.json"
      provides: "All English message keys including new ones for project page, history, canvas, items"
    - path: "messages/de.json"
      provides: "All German message keys matching en.json"
  key_links:
    - from: "src/lib/utils/format.ts"
      to: "$lib/paraglide/runtime"
      via: "getLocale() for Intl.NumberFormat locale"
      pattern: "getLocale\\(\\)"
    - from: "src/lib/utils/format.ts"
      to: "$lib/paraglide/messages"
      via: "m.time_* message functions for relative time"
      pattern: "m\\.time_"
    - from: "src/lib/utils/currency.ts"
      to: "$lib/paraglide/runtime"
      via: "getLocale() for Intl.NumberFormat locale"
      pattern: "getLocale\\(\\)"
---

<objective>
Create locale-aware formatting utilities and add all missing message keys for the entire string extraction phase.

Purpose: Establish the formatting foundation (number/dimension/time/price) that all subsequent plans depend on, and pre-populate all message keys so Plans 02 and 03 can focus purely on wiring components without modifying message files.

Output: Updated `format.ts` with formatting utilities, updated `currency.ts` with locale-aware price formatting, and complete en.json/de.json with all keys needed for the phase.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-string-extraction/02-RESEARCH.md
@.planning/phases/01-i18n-infrastructure/01-01-SUMMARY.md
@messages/en.json
@messages/de.json
@src/lib/utils/format.ts
@src/lib/utils/currency.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create locale-aware formatting utilities and update currency formatting</name>
  <files>
    src/lib/utils/format.ts
    src/lib/utils/currency.ts
  </files>
  <action>
**format.ts** — Add these exports (keep existing `getInitials`):

1. `formatDecimal(value: number, decimals?: number): string` — Uses `new Intl.NumberFormat(getLocale(), { minimumFractionDigits: decimals, maximumFractionDigits: decimals })`. Default `decimals = 2`. Import `getLocale` from `$lib/paraglide/runtime`.

2. `formatDimension(width: number, height: number): string` — For integers, use plain `String(n)`; for decimals, use `formatDecimal(n, 1)`. Return `` `${w} × ${h} cm` ``.

3. `formatRelativeTime(iso: string): string` — Calculate diff from `Date.now()`. Use existing paraglide message keys:
   - `< 1 min` → `m.time_just_now()`
   - `< 60 min` → `m.time_minutes_ago({ count: minutes })`
   - `< 24 hours` → `m.time_hours_ago({ count: hours })`
   - `≥ 24 hours` → `m.time_days_ago({ count: days })`
   Import `* as m from '$lib/paraglide/messages'`.

**currency.ts** — Update `formatPrice()` to use `Intl.NumberFormat(getLocale(), { minimumFractionDigits: 2, maximumFractionDigits: 2 })` instead of `.toFixed(2)`. Import `getLocale` from `$lib/paraglide/runtime`.
  </action>
  <verify>
Run `bun check` — no type errors in format.ts or currency.ts. Run `bun build` — clean build.
  </verify>
  <done>
`formatDecimal(3.5)` returns "3,50" when locale is "de" and "3.50" when locale is "en". `formatDimension(3.5, 2)` returns "3,5 × 2 cm" in DE. `formatRelativeTime` calls paraglide time_* functions. `formatPrice` uses Intl.NumberFormat.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add all missing message keys to en.json and de.json</name>
  <files>
    messages/en.json
    messages/de.json
  </files>
  <action>
Audit the existing en.json/de.json and add ALL keys needed for the remaining hardcoded strings across the app. The research audit identified these categories:

**For `projects/[id]/+page.svelte`** — Check if these keys already exist before adding. Many were noted as "already exist" in the research. Only add keys that are genuinely missing. Likely missing sidebar action group titles and toggle state labels:
- `project_sidebar_group_collaboration` / "Collaboration" / "Zusammenarbeit"
- `project_sidebar_group_canvas` / "Canvas" / "Leinwand"
- `project_sidebar_toggle_on` / "On" / "An"
- `project_sidebar_toggle_off` / "Off" / "Aus"

**For `history/+page.svelte`** — Again check existing keys first. Likely missing:
- `history_filter_all_items` / "All items" / "Alle Elemente"
- `history_filter_all_users` / "All users" / "Alle Benutzer"
- `history_filter_all_sources` / "All sources" / "Alle Quellen"
- `history_filter_source_user` / "User" / "Benutzer"
- `history_filter_source_mcp` / "MCP" / "MCP"
- `history_column_time` / "Time" / "Zeit"
- `history_column_user` / "User" / "Benutzer"
- `history_column_item` / "Item" / "Element"
- `history_column_action` / "Action" / "Aktion"
- `history_column_field` / "Field" / "Feld"
- `history_column_change` / "Change" / "Änderung"
- `history_loading` / "Loading history..." / "Verlauf wird geladen..."
- `history_empty` / "No change history yet." / "Noch kein Änderungsverlauf."
- `history_no_matches` / "No changes match the current filters." / "Keine Änderungen entsprechen den aktuellen Filtern."
- `history_rows_selected` / "{selected} of {total} row(s) selected" / "{selected} von {total} Zeile(n) ausgewählt"
- `history_previous` / "Previous" / "Zurück"
- `history_next` / "Next" / "Weiter"
- `history_select_all` / "Select all" / "Alle auswählen"
- `history_select_row` / "Select row" / "Zeile auswählen"

**For `FloorplanCanvas.svelte`** — Context menu items, zoom control titles:
- `canvas_rotate_left` / "Rotate Left" / "Links drehen"
- `canvas_rotate_right` / "Rotate Right" / "Rechts drehen"
- `canvas_remove_from_plan` / "Remove from Plan" / "Vom Plan entfernen"
- `canvas_zoom_in` / "Zoom in" / "Vergrössern"
- `canvas_zoom_out` / "Zoom out" / "Verkleinern"
- `canvas_reset_view` / "Reset view" / "Ansicht zurücksetzen"
- `canvas_lock_zoom` / "Lock zoom" / "Zoom sperren"
- `canvas_unlock_zoom` / "Unlock zoom" / "Zoom entsperren"
- `canvas_close_menu` / "Close menu" / "Menü schliessen"
- `canvas_scale_bar` / "100 cm" / "100 cm"

**For `ScaleCalibration.svelte`** — Reuse canvas zoom titles above (same strings).

**For `HistoryActionBadge.svelte`** — Action/source label translation:
- `history_action_create` / "create" / "erstellen"
- `history_action_update` / "update" / "aktualisieren"
- `history_action_delete` / "delete" / "löschen"
- `history_source_user` / "User" / "Benutzer"
- `history_source_mcp` / "MCP" / "MCP"

**For `OnlineUsers.svelte`**:
- `online_users_fallback` / "User" / "Benutzer"
- `online_users_anonymous` / "Anonymous" / "Anonym"

**For `ImageViewer.svelte`**:
- `image_viewer_view_image` / "View image {index}" / "Bild {index} ansehen"

**For item count plural form** (I18N-10):
- `item_list_count` / "{count} {count, plural, one {item} other {items}}" / "{count} {count, plural, one {Element} other {Elemente}}"

**IMPORTANT:** Before adding any key, grep the existing en.json to check if it already exists. Only add genuinely new keys. Keep alphabetical ordering within the JSON if the file is alphabetically sorted, or append at the end matching the existing convention.
  </action>
  <verify>
Run `bun build` — no missing key warnings. Verify en.json and de.json have the same number of keys (count with `grep -c '"' messages/en.json`). Spot check: `grep "history_column_time" messages/en.json` returns a match.
  </verify>
  <done>
Both en.json and de.json contain all message keys needed for Plans 02, 03, and 04. Key count matches between files. Build succeeds without warnings.
  </done>
</task>

</tasks>

<verification>
1. `bun build` succeeds with no warnings about missing message keys
2. `bun check` passes with no type errors
3. `formatDecimal`, `formatDimension`, `formatRelativeTime` exported from format.ts
4. `formatPrice` in currency.ts uses `Intl.NumberFormat`
5. en.json and de.json have matching key counts
</verification>

<success_criteria>
- Formatting utilities exist and are type-safe
- All message keys for the entire phase exist in both locale files
- Build and type-check pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-string-extraction/02-01-SUMMARY.md`
</output>
