---
phase: 02-string-extraction
plan: 03
type: execute
wave: 2
depends_on: ['02-01']
files_modified:
  - src/lib/components/canvas/FloorplanCanvas.svelte
  - src/lib/components/canvas/ScaleCalibration.svelte
  - src/lib/components/canvas/CanvasControls.svelte
  - src/lib/components/items/ItemList.svelte
  - src/lib/components/items/ItemCard.svelte
  - src/lib/components/items/ItemBottomSheet.svelte
  - src/lib/components/items/ImageViewer.svelte
  - src/lib/components/projects/ProjectCard.svelte
  - src/lib/components/projects/ProjectListDialog.svelte
  - src/lib/components/sharing/ShareLinkList.svelte
  - src/lib/components/collaboration/OnlineUsers.svelte
autonomous: true
requirements: [I18N-02, I18N-04, I18N-10]

must_haves:
  truths:
    - 'Canvas context menu, zoom controls, and scale bar display in German when locale is DE'
    - "Canvas dimension labels use locale-correct decimal comma in German (e.g., '3,5 × 2 cm')"
    - 'Item prices display with locale-correct decimal formatting'
    - 'ProjectCard relative time displays in German when locale is DE'
    - 'Date/time calls use explicit getLocale() parameter'
    - 'OnlineUsers fallback names display in German'
    - "Item count uses correct plural form ('1 Element', '2 Elemente' in DE)"
  artifacts:
    - path: 'src/lib/components/canvas/FloorplanCanvas.svelte'
      provides: 'Translated canvas text (context menu, zoom, dimensions, scale)'
      contains: 'import.*paraglide/messages'
    - path: 'src/lib/components/items/ItemList.svelte'
      provides: 'Locale-formatted total cost and plural item count'
      contains: "formatPrice\\|formatDecimal"
    - path: 'src/lib/components/projects/ProjectCard.svelte'
      provides: 'Translated relative time on project cards'
      contains: 'formatRelativeTime'
  key_links:
    - from: 'src/lib/components/canvas/FloorplanCanvas.svelte'
      to: 'src/lib/utils/format.ts'
      via: 'formatDimension for canvas dimension labels'
      pattern: 'formatDimension'
    - from: 'src/lib/components/items/ItemCard.svelte'
      to: 'src/lib/utils/currency.ts'
      via: 'formatPrice for locale-aware price display'
      pattern: 'formatPrice'
    - from: 'src/lib/components/projects/ProjectCard.svelte'
      to: 'src/lib/utils/format.ts'
      via: 'formatRelativeTime replacing hand-rolled English relative time'
      pattern: 'formatRelativeTime'
---

<objective>
Wire all remaining components to paraglide messages and locale-aware formatting utilities.

Purpose: Complete the string extraction across canvas components (context menu, zoom controls, dimension labels, scale bar), item components (prices, dimensions, counts), and miscellaneous components (ProjectCard relative time, dates, fallback names). This covers all non-route-page components.

Output: Every component file uses `m.*()` for user-visible strings and `formatDecimal`/`formatDimension`/`formatPrice`/`formatRelativeTime` for locale-sensitive values.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-string-extraction/02-RESEARCH.md
@.planning/phases/02-string-extraction/02-01-SUMMARY.md
@src/lib/utils/format.ts
@src/lib/utils/currency.ts
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire canvas components (FloorplanCanvas, ScaleCalibration, CanvasControls)</name>
  <files>
    src/lib/components/canvas/FloorplanCanvas.svelte
    src/lib/components/canvas/ScaleCalibration.svelte
    src/lib/components/canvas/CanvasControls.svelte
  </files>
  <action>
**FloorplanCanvas.svelte** (1402 lines — largest file, be thorough):

1. Add `import * as m from '$lib/paraglide/messages'` and `import { formatDimension, formatDecimal } from '$lib/utils/format'`.

2. **Context menu items**: Replace `'Rotate Left'` → `m.canvas_rotate_left()`, `'Rotate Right'` → `m.canvas_rotate_right()`, `'Remove from Plan'` → `m.canvas_remove_from_plan()`.

3. **Zoom control titles**: Replace `title="Zoom in"` → `title={m.canvas_zoom_in()}`, `title="Zoom out"` → `title={m.canvas_zoom_out()}`, `title="Reset view"` → `title={m.canvas_reset_view()}`, `title="Lock zoom"`/`"Unlock zoom"` → `title={zoomLocked ? m.canvas_unlock_zoom() : m.canvas_lock_zoom()}` (or however the toggle is structured).

4. **Close menu aria-label**: Replace `'Close menu'` → `m.canvas_close_menu()`.

5. **Scale bar text**: Replace `'100 cm'` or similar → `m.canvas_scale_bar()`.

6. **Dimension labels on canvas**: Replace `${item.width} × ${item.height} cm` template literals (used in Konva `<Text text={...}>` props) with `formatDimension(item.width, item.height)`.

7. **Distance indicators**: Replace `${Math.round(indicator.distanceCm)} cm` with `` `${formatDecimal(Math.round(indicator.distanceCm), 0)} cm` `` — or since `Math.round` produces integers, this may just need the "cm" unit (integers don't need locale formatting). Use `formatDecimal(indicator.distanceCm, 0)` to be safe with locale thousands separator.

**ScaleCalibration.svelte**:

1. Add `import * as m from '$lib/paraglide/messages'` (if not already present).

2. Replace zoom control titles: `title="Zoom in"` → `title={m.canvas_zoom_in()}`, `title="Zoom out"` → `title={m.canvas_zoom_out()}`, `title="Reset view"` → `title={m.canvas_reset_view()}`. Reuse the same canvas\_\* keys.

**CanvasControls.svelte**:

1. Add `import { formatDecimal } from '$lib/utils/format'` (if needed).

2. Replace `scale.toFixed(2)` in the scale percentage display with `formatDecimal(scale, 2)` or `formatDecimal(scale * 100, 0)` depending on how scale is displayed. Check the actual code to determine the correct approach.
   </action>
   <verify>
   Run `bun check` — no type errors. Run `bun build` — clean build. Verify: `grep -n "Rotate Left\|Rotate Right\|Remove from Plan\|Zoom in\|Zoom out\|Reset view" src/lib/components/canvas/FloorplanCanvas.svelte` should return no hardcoded English strings (only `m.*()` calls).
   </verify>
   <done>
   Canvas context menu, zoom controls, scale bar, dimension labels, and distance indicators all use paraglide messages or locale-aware formatting. No hardcoded English remains in canvas components.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Wire item components and remaining miscellaneous components</name>
  <files>
    src/lib/components/items/ItemList.svelte
    src/lib/components/items/ItemCard.svelte
    src/lib/components/items/ItemBottomSheet.svelte
    src/lib/components/items/ImageViewer.svelte
    src/lib/components/projects/ProjectCard.svelte
    src/lib/components/projects/ProjectListDialog.svelte
    src/lib/components/sharing/ShareLinkList.svelte
    src/lib/components/collaboration/OnlineUsers.svelte
  </files>
  <action>
**ItemList.svelte**:
1. Replace `totalCost.toFixed(2)` with `formatPrice(totalCost, currencyCode)` (import from currency.ts) or `formatDecimal(totalCost)` depending on context. Check how price is displayed.
2. If item count is displayed (e.g., "3 items"), replace with `m.item_list_count({ count: items.length })` to get proper German plural forms ("3 Elemente").

**ItemCard.svelte**:

1. Replace `item.price.toFixed(2)` with `formatPrice(item.price, currencyCode)` — import `formatPrice` from `$lib/utils/currency`. Check how currency code is available (prop or store).

**ItemBottomSheet.svelte**:

1. Replace `item.price.toFixed(2)` with `formatPrice(item.price, currencyCode)`.
2. Replace `${item.width} × ${item.height} cm` with `formatDimension(item.width, item.height)`.

**ImageViewer.svelte**:

1. Add `import * as m from '$lib/paraglide/messages'`.
2. Replace `aria-label="View image {i + 1}"` with `aria-label={m.image_viewer_view_image({ index: i + 1 })}`.

**ProjectCard.svelte**:

1. Add `import { formatRelativeTime } from '$lib/utils/format'`.
2. Remove the local hand-rolled `formatRelativeTime()` or `relativeTime()` function that produces English strings like "5m ago".
3. Replace calls to it with the shared `formatRelativeTime()` from format.ts.

**ProjectListDialog.svelte**:

1. Add `import { getLocale } from '$lib/paraglide/runtime'`.
2. Replace `toLocaleDateString(undefined, ...)` or `toLocaleDateString()` with `toLocaleDateString(getLocale(), ...)`.

**ShareLinkList.svelte**:

1. Add `import { getLocale } from '$lib/paraglide/runtime'`.
2. Replace `toLocaleString()` with `toLocaleString(getLocale())`.

**OnlineUsers.svelte**:

1. Add `import * as m from '$lib/paraglide/messages'`.
2. Replace `'User'` fallback with `m.online_users_fallback()`.
3. Replace `'Anonymous'` with `m.online_users_anonymous()`.
   </action>
   <verify>
   Run `bun check` — no type errors. Run `bun build` — clean build. Verify: `grep -rn "\.toFixed(2)" src/lib/components/items/` should return no matches (all replaced with locale-aware formatting). `grep -n "ago" src/lib/components/projects/ProjectCard.svelte` should return no English time suffixes.
   </verify>
   <done>
   All item components use locale-aware price and dimension formatting. ProjectCard uses shared formatRelativeTime. Date calls use explicit getLocale(). OnlineUsers shows translated fallback names. ImageViewer has translated aria-label. Item count uses plural-aware message key.
   </done>
   </task>

</tasks>

<verification>
1. `bun build` and `bun check` pass cleanly
2. No `.toFixed(2)` calls remain in item components
3. No hand-rolled relative time remains in ProjectCard
4. Canvas dimension labels use formatDimension
5. All date formatting uses explicit getLocale() parameter
6. Plural form works for item counts
</verification>

<success_criteria>

- Every component uses paraglide messages for user-visible text
- All number/price/dimension formatting is locale-aware via Intl.NumberFormat
- Relative time consolidated to shared utility
- Build and type-check pass cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/02-string-extraction/02-03-SUMMARY.md`
</output>
